[loggers]
keys=root

[handlers]
keys=stream

[formatters]
keys=simple

[formatter_simple]
format=%(message)s

[handler_stream]
class=StreamHandler
formatter=simple
args=(sys.stdout,)

[logger_root]
level=DEBUG
handlers=stream
